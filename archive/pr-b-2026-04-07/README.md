# PR-B Archive Extension (2026-04-07)

This archive snapshot stores uncertain or legacy artifacts discovered during the PR-B pass.

## Archived items

- `legacy-assets/build_and_copy.bat`: machine-specific Windows helper script with hard-coded local paths.
- `legacy-assets/frontend-public-icons/`: historical favicon/icon variants (`1/` and `2/`) that are not referenced by the current app entry points.
- `legacy-assets/favicon_io.zip`: legacy favicon bundle not required for runtime.

## Notes

These files are retained for traceability and potential recovery, but intentionally moved out of active app paths.
